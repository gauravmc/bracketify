<% teams ||= [] %>
<% winner ||= -1 %>
<% name ||= "Match #{index + 1}" %>

<div class="row">
<div class="col-xs-12">
  <div class="panel panel-default">
    <div class="panel-heading"><%= name %></div>
    <div class="panel-body">
      <div class="group-sort">
        <% teams.each do |team| %>
          <div class="group-country">
            <%= team_with_flag(team, winner == team) %>
            <div class="clearfix"></div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
</div>
